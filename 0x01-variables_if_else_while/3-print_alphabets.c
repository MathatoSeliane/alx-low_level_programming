#include <stdio.h>
/**
 * main - Print the alphabet in lower and uppercase.
 * Return: 0 (Success)
 */
int main(void)
{
	char lcs;

	for (lcs = 'a'; lcs <= 'z'; lcs++)
	{
		putchar(lcs);
	}
	putchar ('\n");
	return (0);
}
